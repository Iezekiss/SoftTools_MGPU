
# Практическая работа № 3

**Тема:** Консолидация и анализ данных из различных источников
**Вариант:** 11
**Студент:** Кузьмина Д. Ю.
**Дисциплина:** DCCA DBA (Data Collection, Consolidation and Analytics)

---

## 🎯 Цель работы

Целью практической работы является освоение процесса интеграции и анализа данных, поступающих из различных источников — файлов форматов CSV, Excel и JSON.
В ходе выполнения работы требуется:

* разработать скрипт для генерации и загрузки данных;
* провести предобработку и консолидацию таблиц;
* рассчитать общий доход студентов (стипендия + призовые);
* визуализировать результаты анализа;
* подготовить SQL-скрипты для демонстрации структуры и запросов;
* упаковать проект в Docker-контейнер для воспроизводимости.

---

## 🧩 Используемое ПО

* **Python 3.10**
* **Pandas**, **Matplotlib**, **Seaborn**, **OpenPyXL**, **Jupyter**
* **Docker + docker-compose**
* **VS Code** для редактирования и запуска скриптов

---

## 📂 Структура проекта

```
kuzmina_VAR11/
│
├── main_analysis.py          # основной скрипт (выполняет все этапы)
├── requirements.txt          # список зависимостей Python
├── README.md                 # описание проекта
├── docker_help.md            # инструкция по Docker
├── Dockerfile                # конфигурация контейнера
├── docker-compose.yml        # запуск через docker-compose
│
├── data/                     # исходные наборы данных
│   ├── students.csv
│   ├── scholarships.xlsx
│   └── olympiads.json
│
├── results/                  # результаты анализа и визуализации
│   ├── merged_dataset.csv
│   ├── income_summary.csv
│   ├── income_distribution.png
│   ├── income_by_faculty.png
│   └── income_by_year.png
│
└── sql/                      # SQL-скрипты
    ├── create_tables.sql
    └── income_query.sql
```

---

## 🧠 Ход выполнения

### 1. Подготовка данных

Были сгенерированы три набора данных:

* **students.csv** — `student_id`, `faculty`, `enrollment_year`;
* **scholarships.xlsx** — `faculty`, `scholarship_amount`;
* **olympiads.json** — `student_id`, `prize_amount`.

Данные имитируют сведения из разных источников (учебный отдел, бухгалтерия, олимпиадный комитет).

### 2. Загрузка и предобработка

Данные были импортированы в `pandas`-DataFrame.
Проведена проверка типов, пропусков и дубликатов.
Названия столбцов приведены к стилю `snake_case`.

### 3. Консолидация и обогащение

Объединение таблиц выполнено через `pd.merge()`:

1. `students` ↔ `scholarships` по полю `faculty`;
2. полученный результат ↔ `olympiads` по `student_id`.

Добавлен новый столбец:

```python
merged["total_income"] = merged["scholarship_amount"] + merged["prize_amount"]
```

### 4. Анализ и визуализация

Построены три ключевые визуализации:

1. **Распределение общего дохода студентов** (`income_distribution.png`)
2. **Средний доход по предметам** (`income_by_faculty.png`)
3. **Доход по годам поступления** (`income_by_year.png`)

Графики показали, что:

* основная часть студентов получает базовую стипендию 1000–1800;
* у части студентов доход увеличивается за счёт призов до 3500–3800;
* направления *Mathematics* и *Informatics* демонстрируют наибольшие средние значения.

### 5. SQL-скрипты

Автоматически сгенерированы два файла:

* **create_tables.sql** — описание структуры трёх таблиц и связей;
* **income_query.sql** — запрос для объединения таблиц и расчёта `total_income` с агрегацией среднего дохода по предметам.

---

## 🐳 Docker-окружение

Проект полностью контейнеризован.
Файлы `Dockerfile` и `docker-compose.yml` обеспечивают развёртывание среды с Python 3.10 и всеми зависимостями.

### Сборка и запуск

```bash
# сборка образа
docker-compose build

# запуск Jupyter Notebook
docker-compose up
```

После запуска откроется ссылка:

```
http://127.0.0.1:8888/?token=...
```

Перейдите по ней для работы с проектом в браузере.
Подробная инструкция приведена в файле [docker_help.md](./docker_help.md).

---

## 📊 Результаты

В результате выполнения работы были получены:

* корректно объединённые данные по 100 студентам;
* рассчитан показатель **total_income** (стипендия + призы);
* сформированы визуализации и SQL-запросы;
* проект упакован в Docker-контейнер.

---

## ✅ Выводы

Проведённый анализ подтвердил корректность консолидации данных и расчётов.
Разработанный скрипт позволяет автоматически генерировать, обрабатывать и анализировать данные из разных источников.
Использование Docker обеспечивает воспроизводимость и независимость среды, что соответствует современным требованиям к аналитическим решениям.

